import { ITestSuite } from "@/lib/types";

interface TestSuiteDisplayProps {
  testSuite?: ITestSuite;
}
export const TestSuiteDisplay: React.FC<TestSuiteDisplayProps> = ({}) => {
  // TODO: have a specialized thing here to display the 
  return null;
};
